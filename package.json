{
  "name": "automation-studio",
  "private": true,
  "version": "1.5.1",
  "description": "Automation Studio \u2014 the Electron desktop app shell (frontend/) that wraps the autonomous Python automation engine (backend/). This root is the canonical entrypoint; the engine in backend/ also runs standalone (pip install -e ./backend).",
  "author": "beltromatti",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "build:mac": "npm --prefix frontend run build:mac",
    "build:win": "npm --prefix frontend run build:win",
    "build:linux": "npm --prefix frontend run build:linux",
    "frontend:install": "npm --prefix frontend install",
    "frontend:ci": "npm --prefix frontend ci"
  }
}
